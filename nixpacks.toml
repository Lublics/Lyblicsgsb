[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Packages.composer"]

[phases.build]
cmds = ["composer install --ignore-platform-reqs || true"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-3000} -t /app /app/router.php"
